default:
  image: registry.gitlab.com/eventsflow/docker/dev:0.6.0-ubuntu

stages:
- build
- test
- release

test_eventflow:
  stage: test
  script:
  - /usr/sbin/dev/run-pytests.sh
  artifacts:
    reports:
      junit: report.xml
